{
  "name": "example",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "lint": "make",
    "generate": "yarn lint && node script",
    "markdown": "yarn lint && node markdown"
  },
  "dependencies": {
    "snippets": "file:../snippets"
  }
}
