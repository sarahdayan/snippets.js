{
  "name": "example",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "postinstall": "make install && cd ./node_modules/snippets.js && yarn build",
    "lint": "make run",
    "log": "yarn lint && node demos/log",
    "markdown": "yarn lint && node demos/markdown",
    "yaml": "yarn lint && node demos/yaml"
  },
  "dependencies": {
    "camelize": "^1.0.0",
    "lodash.groupby": "^4.6.0",
    "snippets.js": "file:../snippets.js",
    "write-yaml-file": "^3.0.1"
  }
}
